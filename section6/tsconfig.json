{
	"compilerOptions": {
		"target": "ESNext",
		"module": "ESNext",
		"outDir": "dist",
		"strict": true,
		// null 타입이 아닌 변수에 null 값을 할당할 때 허용여부
		// "strictNullChecks": false,
		"moduleDetection": "force",
		// 20버전 이상 업데이트 이후 타입검사 오류 발생시 아래 옵션 추가
		"skipLibCheck": true
	},
	"ts-node": {
		"esm": true
	},
	"include": ["src", "src/chapter0.js"]
}
